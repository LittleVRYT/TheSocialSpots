
// Component has been simplified to not render anything
// since we're no longer supporting dark mode

export function ThemeToggle() {
  // Return null as we don't need this component anymore
  return null;
}
